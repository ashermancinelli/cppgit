#include <vector>
#include <string>
using std::string;

namespace cppgit
{

void handle_init(std::vector<string> args);
// handle_init(std::vector<std::string> args);
// handle_init(std::vector<std::string> args);
// handle_init(std::vector<std::string> args);
// handle_init(std::vector<std::string> args);

}
